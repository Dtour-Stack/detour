{
	"name": "@detour/tray",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"prepare:pglite": "bun scripts/prepare-pglite-assets.ts",
		"prepare:icons": "bun scripts/build-icons.ts",
		"prepare": "bun run prepare:icons && bun run prepare:pglite",
		"start": "bun run prepare && electrobun dev",
		"dev": "bun run prepare && electrobun dev --watch",
		"build:dev": "bun run prepare && electrobun build --env=dev",
		"build:canary": "bun run prepare && electrobun build --env=canary",
		"build:stable": "bun run prepare && electrobun build --env=stable"
	},
	"dependencies": {
		"@detour/core": "workspace:*",
		"@detour/shared": "workspace:*",
		"@electric-sql/pglite": "*",
		"electrobun": "1.18.1"
	}
}
